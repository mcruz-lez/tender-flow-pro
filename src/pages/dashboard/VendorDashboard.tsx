import PageTemplate from "@/components/PageTemplate";

const VendorDashboard = () => {
  return (
    <PageTemplate
      title="Vendor Dashboard"
      description="Manage your vendor profile and track opportunities"
    />
  );
};

export default VendorDashboard;
