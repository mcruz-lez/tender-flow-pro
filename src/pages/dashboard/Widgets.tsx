import PageTemplate from "@/components/PageTemplate";

const Widgets = () => {
  return (
    <PageTemplate
      title="Dashboard Widgets"
      description="Customize your dashboard with interactive widgets"
    />
  );
};

export default Widgets;
