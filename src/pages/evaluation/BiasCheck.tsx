
import PageTemplate from "@/components/PageTemplate";

const BiasCheck = () => {
  return (
    <PageTemplate
      title="Bias Detection"
      description="Detect and prevent bias in evaluation processes"
    />
  );
};

export default BiasCheck;
