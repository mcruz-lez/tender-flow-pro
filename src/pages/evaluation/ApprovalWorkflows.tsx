import PageTemplate from "@/components/PageTemplate";

const ApprovalWorkflows = () => {
  return (
    <PageTemplate
      title="Approval Workflows"
      description="Configure and manage approval processes"
    />
  );
};

export default ApprovalWorkflows;
