
import PageTemplate from "@/components/PageTemplate";

const VendorPortal = () => {
  return (
    <PageTemplate
      title="Vendor Portal"
      description="Self-service portal for vendor management and opportunities"
    />
  );
};

export default VendorPortal;
