
import PageTemplate from "@/components/PageTemplate";

const TenderDocs = () => {
  return (
    <PageTemplate
      title="Tender Documents"
      description="Manage documents related to tender processes"
    />
  );
};

export default TenderDocs;
