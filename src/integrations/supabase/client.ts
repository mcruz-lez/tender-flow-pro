// This file is automatically generated. Do not edit it directly.
import { createClient } from "@supabase/supabase-js";
import type { Database } from "./types";

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;

if (typeof window !== "undefined") {
  // Debug: Log env variables in browser
  console.log("[Supabase] VITE_SUPABASE_URL:", SUPABASE_URL);
  console.log("[Supabase] VITE_SUPABASE_ANON_KEY:", SUPABASE_ANON_KEY);
}

if (!SUPABASE_URL || !SUPABASE_ANON_KEY) {
  console.error("Supabase environment variables are missing. Check your .env file and restart the server.");
}

export const supabase = createClient<Database>(
  SUPABASE_URL,
  SUPABASE_ANON_KEY,
);
